#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

#include "TAD_Cache.h"

int main() {
    MemoryBlock *RAM, *cache1, *cache2, *cache3;

    int ram;
    srand(time(NULL));
    return 0;
}
